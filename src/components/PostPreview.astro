---
import { type CollectionEntry } from 'astro:content';
import ArrowRight from '../assets/icons/arrow-right.svg';
import FormattedDate from './FormattedDate.astro';

type Props = { post: CollectionEntry<'blog'>; class?: string; headingLevel?: 'h2' | 'h3' };

const { post, class: className, headingLevel = 'h2' } = Astro.props;
const { title, publishDate, updatedDate, excerpt } = post.data;
const TitleTag = headingLevel;
---

<div class:list={['post-card spotlight-card group relative flex flex-col sm:flex-row sm:items-stretch gap-6 p-6', className]}>
    <div class="spotlight-glow"></div>
    <!-- Base Link Overlay -->
    <a href={`/blog/${post.id}/`} class="absolute inset-0 z-10" aria-label={`Read ${title}`}></a>
    
    {/* Content Section */}
    <div class="flex-1 min-w-0">
        <a href={`/blog/${post.id}/`} class="block">
            <TitleTag class="mb-3 font-serif text-2xl font-bold leading-tight line-clamp-2 text-main">
                <span class="premium-line-hover">{title}</span>
            </TitleTag>
            {excerpt && <div class="mt-2 text-sm leading-relaxed text-main/90 font-sans max-w-2xl">{excerpt}</div>}
        </a>
    </div>

    {/* Metadata Section */}
    <div class="flex flex-row sm:flex-col justify-between sm:items-end gap-4 shrink-0 pt-1 sm:border-l sm:border-main-border/10 sm:pl-8">
        <div class="text-[11px] font-bold tracking-[0.2em] text-main/40 uppercase font-sans">
            {publishDate.toLocaleDateString('en-US', { month: 'short', year: 'numeric' })}
        </div>
        
        <div class="flex items-center gap-1.5 px-1 sm:px-0">
            {post.data.medium && (
                <a 
                    href={post.data.medium} 
                    target="_blank" 
                    rel="noopener noreferrer" 
                    class="group/icon premium-tooltip relative z-20 flex h-8 w-8 items-center justify-center transition-all duration-300 hover:scale-115" 
                    data-tooltip="Read on Medium"
                >
                    <div class="absolute inset-0 rounded-xl bg-main/5 opacity-0 transition-all duration-300 group-hover/icon:opacity-100 group-hover/icon:scale-110 group-hover/icon:bg-main/10 ring-1 ring-main/10"></div>
                    <img src="https://cdn.simpleicons.org/medium" class="h-3.5 w-3.5 dark:hidden opacity-70 group-hover:opacity-100 transition-all relative z-10" alt="Medium" />
                    <img src="https://cdn.simpleicons.org/medium/white" class="h-3.5 w-3.5 hidden dark:block opacity-70 group-hover:opacity-100 transition-all relative z-10" alt="Medium" />
                </a>
            )}
        </div>
    </div>
</div>
